import React from 'react'

export default function RepoInfo(
    id,
    Name, 
    URL, 
    forks, 
    stargazers_count, 
    watchers_count,
    updated_at, 
    created_at,
    html_url
    ) {
        return (
            <div style={{color: "white"}}>RepoInfo</div>
        )
}
